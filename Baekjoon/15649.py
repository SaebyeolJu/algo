# 15649 - backtracking
# https://www.acmicpc.net/problem/15649

import sys

def testCase():
    input_val = list(map(int, sys.stdin.readline().split()))
    N, M = input_val
    check = False * (N+1)
    ans = []
def check(idx, next):
    if ans == m:
        print(*ans)
        return
    for i in range():
        if not check[i]:
            check[i] = Truedfs(idx + 1, next)
            ckech[i] = False

check(0,0)
